#include <stdint.h>
#include <stdbool.h>

extern int32_t i;              // UndCC_Valid - external declaration in a header file
extern void worker(bool flag); // UndCC_Valid - external declaration in a header file
